# Tasks

- [ ] Export the OpenCode free allowlist from `server.ts`
- [ ] Add `openCodeAdapter` to `defaultAdapters()`
- [ ] Default `allowedModels` to Kilo primary + those three OpenCode ids
- [ ] Tests: advertised ids, `auto` stays Kilo, other OpenCode ids 404, no retry
- [ ] Reload `com.dani-free` and prove `/v1/models` plus one Muse completion
